{
  "name": "voc-research-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js && node --check public/app.js",
    "test": "node ../voctools/tests/run.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
