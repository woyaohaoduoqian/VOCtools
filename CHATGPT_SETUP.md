# 在 ChatGPT 内配置 VOC 调研智能体

这是不依赖你自有 OpenAI API Key 的使用方式：智能体在 ChatGPT 内运行，先验证研究流程，再接入外部采集工具。

## 1. 创建智能体

在 ChatGPT 的 GPT 创建页新建一个 GPT，并将 `soul.md` 全文粘贴到“Instructions / 指令”。随后把以下目录作为知识文件上传（每个目录先压缩为 ZIP）：

- `research-planning/`
- `data-collection/`
- `quality-check/`
- `insight-writing/`

如果 GPT 创建页支持将技能目录直接导入，则优先导入 `SKILL.md` 与相邻资源文件，保持目录结构不变。

## 2. 开启能力

- 开启文件上传与数据分析/代码执行：用于读取 CSV、复算质量指标和生成交付文件。
- 网页浏览只用于规划阶段的公开来源侦察；它不是数据采集的替代品。
- 在尚未接入经授权的采集工具前，关闭或不启用任何会自动执行外部操作的 Action。

## 3. 首轮验收用语

在新对话中发送：

> 帮我看美国 Instagram 上便携榨汁杯的用户痛点，用来确定下一代产品改进方向。

正确行为是先补齐边界、用户、时间窗、预算、产出和种子信息，并只输出待确认方案；不得直接给“用户痛点结论”，不得开始采集。

## 4. 后续接入采集

当需要真实 Instagram 数据时，再单独添加受控工具后端。后端应保存 Apify 凭证，展示试跑范围和预估费用，且只在用户明确确认后执行。ChatGPT 的推理能力不能替代数据源，也不能把网页检索结果伪装成采集结果。
